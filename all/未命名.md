```mermaid
classDiagram
    class BaseEntity {
        +Long id
        +LocalDateTime  createTime
        +LocalDateTime  updateTime
    }
    
    class BaseMapper~T~ {
        <<interface>>
        +save(T entity) int
        +update(T entity) int
        +deleteById(Long id) int
        +sById(Long id) T
    }
    
    class Customer {
        +String customerName
        +String idCard
        +String phone
        +Integer creditLevel
    }
    
    class LoanInfo {
        +Long customerId
        +BigDecimal loanAmount
        +BigDecimal interestRate
        +Integer loanTerm
        +Integer loanStatus
    }
    
    class CustomerMapper {
        <<interface>>
        +findByName(String name) List~Customer~
        +findByIdCard(String idCard) Customer
    }
    
    class LoanInfoMapper {
        <<interface>>
        +findByCustomerId(Long customerId) List~LoanInfo~
        +findByStatus(Integer status) List~LoanInfo~
    }
    
    BaseEntity <|-- Customer
    BaseEntity <|-- LoanInfo
    BaseMapper~BaseEntity~ <|.. CustomerMapper
    BaseMapper~BaseEntity~ <|.. LoanInfoMapper
```